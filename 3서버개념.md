## 비동기

- 하나의 작업이 완료될 때까지 실행 흐름을 막지 않고 기다리는 동안 다른 작업을 병행하여 처리하는 컴퓨터 프로그래밍 동작 방식
- 메인 스레드가 하나뿐인 유니티 에디터에서 그 하나뿐인 스레드를 기다림에 낭비하지 않기 위해 서버와 통신하는 거의 모든 작업은 비동기로 처리됨

## 패킷

- 네트워크상에서 데이터를 주고받을 때 효율적이고 안전한 전송을 위해 쪼개 놓은 데이터 조각(소화물)
- 헤더 + 바디 구조로 이루어짐
    - 헤더
        - 출발지 IP 주소, 목적지 IP 주소
        - 전체 데이터 중 이 조각이 몇 번째 조각인지 나타내는 순서 번호
        - 데이터 손실 여부를 검사하는 오류 체크 코드
    - 바디: 쪼개진 진짜 데이터 일부
- 데이터를 쪼개서 보내는 이유: 중간에 데이터 하나가 손상되어도 망가진 그 1개 패킷만 다시 재전송하면 되므로 훨씬 효율적임

## 프로토콜

- 컴퓨터나 네트워크 장비들이 서로 데이터를 주고받기 위해 미리 정해놓은 통신 규약
- 제주사나 OS가 다른 컴퓨터들이 통신하려면 통일된 규격이 필요함 → 프로토콜로 해결

### HTTP

- 응용 프로그램(웹 브라우저, 앱, 게임 클라이언트 등)과 가까운 위치에 있는 프로토콜
- ‘HTTP 규약에 맞춰 적은 메세지’ 이 자체를 HTTP 라고 부르기도 함
- 소켓이라는 도구를 바탕으로 만들어진 상위 수준이 대화 규칙
- 기본 동작 구조: 요청과 응답
    - 요청: 클라이언트가 서버에 무언가를 요구함
    ex) 서버야 이 프롬프트좀 받아
    - 응답: 서버가 요청을 처리한 후 결과 반환
    ex) 알았어 Claude가 생성한 답변 여깄어
- 특징
    - 무상태성(Stateless)
        - HTTP는 기본적으로 이전 요청 기억하지 않음
        - 매 요청마다 내가 누구인지 인증 정보(API 키 등)를 매번 편지에 적어서 보내야 하는 이유임
    - 요청-응답 방식
        - 요청/응답 자체는 끝나도 TCP 연결은 재사용을 위해 유지될 수 있음
        - 필요할 때만 연결을 맺고 끊으므로 서버의 부담을 줄임
- HTTP의 3가지 구성 요소

| **구성 요소** | **역할** | **비유** |
| --- | --- | --- |
| **1. Start Line (시작 줄)** | 이 요청/응답이 무엇을 하려는지 첫 줄에 핵심만 표시 | 편지 봉투 표지 메세지 ("배달해 주세요", "잘 받았습니다") |
| **2. Header (헤더)** | 본문 데이터의 타입, 인증 정보 등 각종 부가 데이터 | 편지 봉투에 적힌 받는 사람 주소, 우표, 등기 종류 |
| **3. Body (본문)** | 실제로 전달하고자 하는 핵심 데이터 (JSON, HTML, 이미지 등) | 편지 봉투 안의 진짜 편지 내용물 |
- Anthropic 공식 문서에 HTTP Request 헤더 작성법이 아래처럼 안내되어 있음

```csharp
[Anthropic Official API Reference]

Headers required:
- x-api-key: <YOUR_API_KEY>         (필수: 발급받은 API 키)
- anthropic-version: 2023-06-01     (필수: API 버전 날짜)
- content-type: application/json    (필수: 데이터 형식)
```

## 소켓

- 프로그램과 네트워크(인터넷) 사이를 연결해주는 창구 또는 전화기
- 회사(내 컴퓨터)에서 외부 고객(Claude 서버) 에게 전화 걸려면 전화기 필요
→ 내 프로그램이 인터넷으로 데이터를 보내거나 받을 때 OS로부터 전화기(소켓)를 한 대 빌려서 통신 진행

### 소켓 고갈

- 통신할 때 쓸 수 있는 전화기(소켓)가 바닥나서 더 이상 전화를 걸 수 없는 상태
- OS가 한 번에 만들 수 있는 전화기(소켓)의 개수는 한정되어 있음
- 바닥나는 이유 (잘못된 코드의 예시)
    1. 요청할 때마다 새 전화기를 개통함 (`new HttpClient()`)
    Claude API로 검수 요청을 보낼 때마다 새로 전화기를 만듦
    2. 통화가 끝난 후 전화를 끊음 (`Dispose()`)
    "통화 끝났으니 끊을게요" 하고 전화기를 놓음
    3. 하지만 OS는 즉시 정리하지 않음 (`TIME_WAIT`)
    인터넷 특성상 뒤늦게 도착하는 데이터 패킷이 있을 수 있어서, OS는 통화가 끝나도 약 1~4분 동안 전화기를 들고 '대기(TIME_WAIT)' 상태 유지
    4. 결과:
    짧은 시간 동안 요청이 수백, 수천 번 몰리면 모든 전화기가 '대기 상태'로 묶여 버림
    이때 새로운 요청을 보내려고 하면 "사용할 수 있는 전화기(소켓)가 없습니다!" 하면서 에러(`SocketException`)가 발생하고 통신이 완전히 먹통됨

## 스키마

- 데이터 청사진(설계도)